# 全組工作進度紀錄（2026-06-21）

## 已完成

- PR #137 merged，前端 `polaris-web` 部署至 `polaris-web-00002-m9c`，100% 流量正常
- `frontend/.env.local` 填入 `GOOGLE_CLIENT_ID`
- R2 確認 `angelali2026888@gmail.com` 已加入 OAuth Test users
- R2 確認 OAuth Client 已加上 `localhost:3000` origins / redirect URI

## 待手動測試（需 Google 登入）

- [ ] 新手 Tour 觸發 / 跳過
- [ ] 對話紀錄刪除：垃圾桶 → 確認卡片 → 刪除後消失，重整不回來
- [ ] 通知頁：點未讀通知 → 樣式變已讀
- [ ] 通知頁訂閱設定：已登入見公司清單可切換；未登入顯示「請先登入」

> 阻擋點：等 R2 私訊 `GOOGLE_CLIENT_SECRET` 後才能跑本地登入測試

## 等待其他組員

| 等誰 | 內容 | Issue |
|------|------|-------|
| R2 | `GOOGLE_CLIENT_SECRET` 私訊 | — |
| **R7（你）** | **審查 PR #138**（冪等 DELETE /history，713 passed） | #138 |
| R3 | `GET /chunk/{source_id}` 後端端點（DocViewer 真實資料） | #115 |
| R3/R4 | `/ask` ticker / doc_type filter 修復（retrieval 精準度） | #100 |
| R2/R3 | 確認 Cloud Run `/ask` 實際 retrieval path 是否已接 semantic / ColPali | #100 |

## 突發狀況

- **2026-06-21 晚**：WayneSHC（R2 組長）GitHub 帳號消失，`WayneSHC/polaris-desk` repo 無法存取
- 本地 clone 完整無缺（`frontend_2026_0620` 最新 commit `206a20b`，remote 分支快照完整）
- Issues 歷史（#100、#115、#116、#136、#137 等）無法從本地還原
- R6 Phase 2B Formal-scope Dry Run 已完成並推上分支 `r6-phase2b-formal-scope-dry-run-20260621`，但 GitHub Web / API 權限異常導致 repo / PR 頁面顯示 404；本機 `git ls-remote` 仍可看到 branch 與 PR ref
- R6 PR #139 的 Security workflow 一度失敗，原因是 `gitleaks` 將 Markdown 報告中的 `off_period=Unknown` 誤判為 generic-api-key；已改為自然語言格式並 amend / force-push，新 commit `6ba889d` 本機重跑 `gitleaks` 為 no leaks found
- 為避免 GitHub 風控惡化，R6 已暫停 Codex Polaris GitHub hourly monitor（狀態 `PAUSED`）
- **明早開會討論**：確認 R2 狀況、決定由誰重建 repo

## 已知 Issues 清單

| # | 標題 | 狀態 | 負責人 | 備註 |
|---|------|------|--------|------|
| #100 | `/ask` 未套用 ticker / doc_type filter — citation 不對題 | ⏳ OPEN | R3 + R4 | BigQuery semantic views 已有進展；但 R6 實測 `/ask` 仍回 stub/BM25，且 payload 未帶 R6 metadata，需確認 Cloud Run 實際 retrieval path |
| #113 | ops — OAuth Client + Cloud Run env | ✅ CLOSED | R2 | 2026-06-18 關閉 |
| #114 | 前端 Google 登入 + `/history` B 級還原 | ✅ CLOSED | R7 + R2 | 2026-06-20 關閉 |
| #115 | `GET /chunk/{source_id}`（引用追蹤器原文溯源） | ⏳ OPEN | R3 | 阻擋 DocViewer 真實資料 + issue #116 收尾 |
| #116 | R7-1 整合追蹤（Google Auth + DocViewer + 訂閱） | ⏳ OPEN | 全組 | 等 #115 落地後即可關閉 |
| #136 | `DELETE /history/{session_id}` 後端端點 | ✅ CLOSED（via PR #138） | R2 | 冪等實作完成 |
| #137 | feat(R7/frontend)：主題動畫 + RWD + Tour + 後端串接 | ✅ MERGED | R7 | 已部署 `polaris-web-00002-m9c`；test plan 有 4 項待 R7 本地登入驗收 |
| #138 | PR：`DELETE /history/{session_id}` 實作（對應 #136） | 🔍 **待 R7 review** | R2 | 713 tests passed；R2 已 request review @angelali2026888-blip |
| #139 | PR：R6 Phase 2B formal-scope dry run report | ⚠️ PR ref 存在但 Web 404 | R6 | 20 題 `/ask` dry run evidence；已修正 gitleaks false positive，最新 commit `6ba889d` |

---

## 各角色進度總覽

### R2 組長（WayneSHC）

| 項目 | 狀態 |
|------|------|
| #113 ops — OAuth Client + Cloud Run env | ✅ closed 2026-06-18 |
| #114 前端 Google 登入 + `/history` B 級還原 | ✅ closed 2026-06-20 |
| PR #137 merge + polaris-web redeploy | ✅ 完成 |
| OAuth Test users + localhost redirect URI 設定 | ✅ 完成 |
| `GOOGLE_CLIENT_SECRET` 私訊給 R7 | ⏳ 待確認收到 |
| #136 `DELETE /history/{session_id}` + 測試 | ✅ CLOSED via PR #138（713 passed） |
| #115 阻擋 issue #116 收尾 | ⏳ 等 R3 |
| ⚠️ GitHub 帳號（WayneSHC）2026-06-21 晚消失 | 待釐清 |

### R3（officehsieh-afk）

| 項目 | 狀態 |
|------|------|
| #115 `GET /chunk/{source_id}` 後端端點 | ⏳ OPEN，ETA / 卡點不知道（R2 已在 issue 催問，尚無回覆） |
| #100 `/ask` ticker 解析 + hard filter + per-ticker retrieve | ⏳ OPEN，進度不知道 |
| #100 `/ask` doc_type filter（需 R4 確認 enum 值） | ⏳ OPEN，進度不知道 |
| Cloud Run `/ask` retrieval path | ⚠️ 需確認是否仍走 `polaris_core.chunks` / `BigQueryStore`，或是否已有接 `v_chunk_semantic`、`v_colpali_pages_semantic`、ColPali |

### R4（holajennytw / Jenny）

| 項目 | 狀態 |
|------|------|
| #100 確認 `polaris_core.chunks.doc_type` 實際 enum 值 | ⏳ OPEN，阻擋 R3 接 doc_type filter |
| `v_colpali_pages_semantic` BigQuery view | ✅ R4 已回報進 BigQuery；此項不再視為主要 blocker |
| semantic / ColPali 是否進 `/ask` 實際路徑 | ⚠️ 尚未由 R6 實測看到，需 R3 / R2 確認 Cloud Run 是否已部署並切到新路徑 |

### R5

| 項目 | 狀態 |
|------|------|
| eval pipeline（`r5/004-eval-pipeline`、`r5/eval-pipeline`） | 已有 branch，詳細狀態待確認 |
| eval CI smoke（`r5/009-eval-ci-smoke`） | 已有 branch，詳細狀態待確認 |

### R6

| 項目 | 狀態 |
|------|------|
| G3 exploratory cloud smoke（8 題）| ✅ 完成，報告在 `docs/r6/test_logs/R6_G3_Exploratory_Cloud_Run_Smoke_2026-06-17.md` |
| 發現 retrieval 精準度問題（5/8 題 citation 不對題） | ✅ 已開 #100 追蹤，待 R3/R4 修復 |
| Phase 2B Formal-scope Dry Run（20 題） | ✅ 完成，報告在 `docs/r6/test_logs/R6_Phase2B_Formal_Scope_Dry_Run_2026-06-21.md`，raw outputs 在 `docs/r6/test_logs/raw_outputs/2026-06-21_phase2b/` |
| Phase 2B 結果 | ⚠️ 20/20 HTTP 200；Pass 0、Needs Review 20、Fail 0；stub/BM25 19、embedding 0、colpali 0；citation payload 未帶 `event_key` / `source_key` / `published_yyyymm`；NFR-031 違規 0 |
| Phase 2B 結論 | ⏸️ 不建議進正式 Phase 2；需等 `/ask` retrieval path 接入 semantic / ColPali，並補 citation metadata 後再重測 |
| `/ask` code path 觀察 | ⚠️ 從最新 main code path 來看，Cloud Run `/ask` 目前似乎仍走原本 `polaris_core.chunks` / `BigQueryStore` 路徑，未看到 `/ask` 已改用 `v_chunk_semantic`、`v_colpali_pages_semantic` 或 ColPali；需請 R3 / R2 確認目前 Cloud Run `/ask` 實際接的是哪條 retrieval path |
| 6/21 狀態釐清 | ✅ `#132` 已進 main，且 R4 已回報 `v_colpali_pages_semantic` 已在 BigQuery；⚠️ 但 Cloud Run `/ask` 實測仍回 `stub / bm25`，response 尚未看到 `event_key` / `source_key` / `published_yyyymm` |
| GitHub 自動監控 | ⏸️ 已暫停 `polaris-github-hourly-monitor`，避免 GitHub 帳號限制期間繼續大量查詢 |

### R7（angelali2026888，本人）

見上方各節。

---

## 本地環境狀態（R7）

- 分支：`frontend_2026_0620`
- 最新 commit：`206a20b chore: 補 .gitignore / .gcloudignore 缺漏項目`
- 工作目錄：乾淨（nothing to commit）
- `frontend/.env.local`：`GOOGLE_CLIENT_ID` ✅，`GOOGLE_CLIENT_SECRET` ⏳
