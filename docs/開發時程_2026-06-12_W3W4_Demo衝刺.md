# 開發時程更新（2026-06-12）— W3/W4 收斂 + 6/25 Demo 衝刺

> **三個硬日期**（PM 拍板，凌駕各角色 spec 原 Day 編號）：
>
> | 截止 | 目標 |
> |---|---|
> | **6/16（二）** | W3 全部 Tasks 完成（= G3 驗收：Eval ≥ 80%、Deep Research ✅、Watchdog 可跑、ColPali 去留拍板） |
> | **6/19（五）** | W4 全部 Tasks 完成（= G4：上雲、4 場景雲端可重現、離線備援） |
> | **6/25（四）** | **團專 Demo + 簡報初稿**完成（含彩排、備援影片） |
>
> 進度依據：各角色 spec 6/4 站會快照 + `main` 6/5–6/12 commits（#44–#63）+ Drive/`docs/r6` 產出。

---

## 0. 全案關鍵路徑（先看這個）

```
R4 入庫 polaris_core（程式已備、未執行）
   ├──> R3 檢索接真資料 ──> R5 真分 Eval ≥80%（G3 硬門檻）
   ├──> R2 Cloud Run 真部署 ──> G4 雲端可重現
   └──> R7 前端接真 API
R7 前端（0 行碼）──> 6/25 Demo 的「臉」
```

🔴 **紅燈 1 — R4 執行入庫**：ingestion pipeline / chunker / VectorStore 程式 6/10 已全部進 main（#59–#61），但 **102 份法說稿尚未實際灌進 `polaris_core`、`make bq-smoke` 未轉綠**。這步不完成，R3/R5/R2/R7 全部只能跑 stub。**→ 6/13（六）前必須跑完。**

🔴 **紅燈 2 — R7 前端 0 commit**：repo 至今無前端碼。6/25 Demo 的所有畫面都靠它。**→ 6/12 當天起工，用 mock JSON 不等後端**（開工指南與 3 個 JSON 契約已備，`/ask`、`/research`、`/alerts` API 都已上線可接）。

🟡 **ColPali（場景 3）— 2026-06-12 拍板：owner 維持 R4、R1 + R2 為 backup**：至今無 POC 產出。處置：① R4 一律以入庫為最優先，入庫轉綠前不碰 ColPali；② **若 6/14 前 R4 仍無暇啟動 POC，backup 接手——R1 整備素材（從 Drive 挑 10–15 份含圖表法說 PDF + 對應圖表題，召集 6/14 砍留判定）、R2 以 2 天 timebox（Colab 免費 GPU）跑技術 POC**；③ 判準不變——圖表題命中率 **≥ 70% 才採用**（R3 於 6/15–16 接入第 4 路），達不到或 6/14 仍無產出即砍場景 3 + ColPali（記 TD-01），檢索定案 BM25 + 向量 + Rerank 3 路。

---

## 1. 各角色：還沒做的事 + 如何達成

### R1 — PM（產品 & Demo）

**已完成**：PRD / 範圍鎖定、GCP 基礎建設代建、6/4 全員站會快照、`polaris_core` ACL 治理（SOP v1.3）。

**未完成（W3，6/16 止）**
- [ ] G3 風險看板每日更新（Eval 當前分 / ColPali 去留 / 阻塞項 owner+ETA）
- [ ] 中段 demo 主持 + 決 AQ-02 / AQ-05（AQ-03 已由 R2 拍板）
- [ ] G3 驗收清單：四條件逐項 owner 簽核（6/16）
- [ ] **ColPali backup（與 R2 共同，見 §0）**：若 R4 6/14 前無暇啟動 POC，負責整備素材（含圖表 PDF 樣本 + 圖表題）並召集 6/14 砍留判定

**未完成（W4，6/19 止）**
- [ ] 3 篇部落格定稿（1、2 篇技術內容 R2 已交：`docs/blog/01`、`02`；第 3 篇 Watchdog 內容等 R3/R4）
- [ ] Landing 文案拍板（統一「Agent-Augmented Research Workflow」）

**Demo 衝刺（6/25 止）**
- [ ] **簡報初稿**（6/25 前；建議 6/22 出大綱、6/24 初稿審完）
- [ ] 5 分鐘腳本 + 彩排 2 次（6/23、6/24）、Demo Day MC（6/25）

**如何達成**：每日站會 15 分鐘只追三件事（R4 入庫了沒 / R5 分數多少 / R7 畫面長怎樣）；6/13 拍板 ColPali 砍留；簡報素材直接取自現成產出（G1–G3 readiness 文件、eval 報告、blog 1–2 篇），不要從零寫。

---

### R2 — AI 架構師 · Tech Lead

**已完成**：W1–W3 全綠（LangGraph 5 節點、Temporal Anchoring、retry、LLMLingua ≥50% 實測、Compliance、Deep Research v0/v1 過驗收、G3 架構面自評 Go）；W4 已預作：Cloud Run prep（#44）、thin FastAPI `/ask`+`/research`（#45）、CORS（#46）、`/alerts`（eac61c7）、Watchdog→通知中心 bridge（#58）、部落格 1、2 技術內容。

**未完成（W4，6/19 止）**
- [ ] **D20–22 真部署**：Cloud Run 上線 + BigQuery 後端 + Secret Manager（runbook 已寫好：`docs/上雲_Cloud_Run_runbook.md`）。唯二前置：R4 入庫完成、全員 GCP ADC 設定
- [ ] D24 G4：4 場景**在雲端**可重現（同場景 10 次一致）

**Demo 衝刺（6/25 止）**
- [ ] 彩排技術待命（6/23–24）、Demo Day 後端待命

**如何達成**：R4 入庫一轉綠（目標 6/13–14），**6/15–16 即部署 Cloud Run**（不等 6/17，留 buffer 給 G4）；部署當天用 `make bq-smoke` + `/healthz` + 一題 e2e 驗收；協助 R3 把 Cohere Rerank 接進 retriever（R3 最弱的一段）。另掛 **ColPali backup（與 R1 共同，見 §0）**：若 R4 6/14 前無暇啟動 POC，R1 整備素材、R2 以 2 天 timebox 跑技術 POC；此任務優先序低於部署與救火，衝突時先保 G4。

---

### R3 — Agent 工程師

**已完成**：retriever v0（#47）+ 2-way 介面（#49，6/5–6/7，本人）；Watchdog Agent Phase 1（#56，specs/003，R2 代寫骨架）。Writer 引用 / Compliance / LLMLingua 由 R2 代寫已就緒。

**未完成（W3，6/16 止）— 本週最忙角色**
- [ ] **檢索接真資料**：R4 入庫後，把 2-way（BM25+向量）接 `polaris_core` 真 chunks、跑通一題真檢索
- [ ] **Cohere Rerank 第 3 路**（`client.v2.rerank`, `rerank-v4.0`）；ColPali 若砍即定案 3 路
- [ ] **Watchdog 強化到 demo 等級**：穩定觸發 + 合規判斷準（Phase 1 骨架已在，接 mock MOPS 事件即可，不等 R4）
- [ ] 新聞評估卡（只描述 / 標證據 / 標矛盾，0 買賣建議）— **若時間不夠，依 spec 降級：先保檢索 + Watchdog，新聞卡延 W4**

**未完成（W4，6/19 止）**
- [ ] Watchdog 接真實事件來源（R4 的 MOPS 事件；不及則維持 demo-only mock）
- [ ] 供部落格 3（Watchdog cron→event）技術內容給 R1

**Demo 衝刺（6/25 止）**
- [ ] 彩排：Agent 場景（Deep Research + Watchdog）連跑不出錯

**如何達成**：不要從零造輪子——retriever 介面、Watchdog 骨架、compliance 全部已在 main，工作是「接線與調準」：① 6/13–14 接真 chunks ② 6/15 Rerank ③ 6/16 Watchdog 用 `tests/` 既有 mock 事件打通 `/alerts` 給 R7。每天和 R5 對「不及格題清單」修檢索。

---

### R4 — 資料工程師（吳瑾瑜）🔴 全案關鍵路徑

**已完成**：語料蒐齊（Drive 102 份法說 + 15 份財報）、`polaris_core` WRITER 權限已開（6/8）、資料下載清單（#48）。ingestion 程式三件套已由 R2 代寫進 main：VectorStore 兩後端（#59）、pipeline sanitize→embed→入庫（#60）、PDF 切塊器（#61）。

**未完成（W3，6/16 止）— 最高優先，全隊都在等**
- [ ] **執行入庫**：102 份法說稿 → chunks → `gemini-embedding-2` → `polaris_core`（程式已備，跑 `python -m polaris.ingestion` 流程即可；遇錯回報 R2 當天修）
- [ ] **`make bq-smoke` 轉綠** → 在群組宣告「資料解鎖」
- [ ] Ontology V1（R6 已凍結）灌進庫
- [ ] Watchdog 事件來源：先交付 mock MOPS 事件 JSON 餵 R3（真爬蟲 W4 再說）
- [ ] ColPali POC（owner 維持 R4、**R1/R2 backup**，2026-06-12 拍板）：入庫轉綠後才啟動；若 6/14 前無暇，交接給 backup（R1 整備素材、R2 跑 POC），R4 不用再管；<70% 或 6/14 無產出即砍（見 §0）
- [ ] 新聞表 + 第 5 路 — **若 6/16 前做不完，降級至 W4 或砍**（spec 允許）

**未完成（W4，6/19 止）**
- [ ] **離線備援資料集**（G4 條件）：pgvector fallback 用匯出向量重建（不重算 embedding）+ mock ReAct trace JSON
- [ ] pgvector fallback 驗證：同一份 eval 兩後端結果一致
- [ ] MOPS 爬蟲 / 事件來源穩定化（不及則 demo-only mock，已有降級路線）

**Demo 衝刺（6/25 止）**
- [ ] 6/23–24 彩排前：備援資料 + mock trace 實切演練一次

**如何達成**：**6/12–13 只做一件事＝入庫**。先 1 份 PDF 跑通全管線（chunker→sanitize→embed→BigQuery），確認 schema/維度（768/cosine）沒問題後批次跑 102 份；估 embedding 成本後一次算完（憲法：算一次、大家讀）。卡任何錯立即丟群組 @R2，不要自己 debug 超過 1 小時。

---

### R5 — Eval Lead（楊宗勲）

**已完成**：Eval pipeline Phase 1（題庫 + runner + smoke 達標率，#57）、Phase 2（ragas_score + 真分報告，285c2e1）、題庫 75 題含 10 紅隊題（3949520）、eval smoke CI gate（#62）、BigQuery 權限已開（6/9）。⚠️ 以上多為 R2/PM 代建——**R5 本人需接手日常跑分與迭代**。

**未完成（W3，6/16 止）— G3 硬門檻在你手上**
- [ ] **題庫 75 → 130 題**（+ 場景 4 獨立 10 題；新聞 / 跨產業題；題目與 golden answer 借 R6 的 Ontology V1 + RedTeam v0.3）
- [ ] **R4 入庫後立刻跑真分**（Phase 2 報告已能標「真分」）：跑分 → 列不及格題 → 回報對應 owner 修 → 再跑，每日一輪
- [ ] 三方 Judge（Claude + GPT + Gemini）投票，**只在 G3 閘門跑**（省 token；平日 CI 用 Flash）
- [ ] 6/16 G3：公布 Eval 分數（目標 ≥ 80%；CP ≥ 0.85 / Faithfulness ≥ 0.90 / AR ≥ 0.85）

**未完成（W4，6/19 止）**
- [ ] Eval 報告（130 題 + 場景 4 成績、圖表、解讀、**買賣建議 = 0 證據**）
- [ ] G4 複跑：雲端後端上線後再確認 ≥ 80%

**Demo 衝刺（6/25 止）**
- [ ] 給 R1「品質數據話術」進簡報（分數圖表直接從 Eval 報告出）

**如何達成**：擴題不要手寫——用 R6 的 Ontology/紅隊庫 + 既有 75 題格式（`src/polaris/eval/data/questions_v0.csv`）半自動生成、R6 校 golden answer；6/13 R4 一解鎖就跑第一輪真分，**留 6/14–16 三天修題迭代**（這是 80% 能不能到的關鍵 buffer）。分數卡關就啟動降級（砍場景 3 題組）。

---

### R6 — 金融品質工程師

**已完成**：**Ontology V1 已凍結**（`docs/r6/ontology/Ontology_V1.xlsx`，#53，6/10）、資料字典 v1.2、標註指引 v0.2、**紅隊題庫 v0.3**（`docs/r6/question_bank/`）、新聞白名單。W1–W2 缺口①③已補上。

**未完成（W3，6/16 止）**
- [ ] **紅隊執行**：拿 RedTeam v0.3 實際攻擊系統（API 已能打 `/ask`），登記違規/幻覺清單交 R2/R3 修 — 0 買賣建議是 SC-003 證據
- [ ] **每日領域審閱**：R4 入庫後每天校 ≥ 10 題系統答案，錯誤清單交對應角色（與 R5 的不及格題清單合流）
- [ ] 與 R5 標註場景 4 獨立 10 題（標註指引 v0.2 已有，照做即可）
- [ ] 新聞來源可信度分級（AQ-09）— 白名單已有，補分級欄即可；新聞路若砍則降優先

**未完成（W4，6/19 止）**
- [ ] Eval 報告金融把關 + 場景 4 解讀
- [ ] G4 複查：4 場景財務數字抽查（≥ 30 題錯誤率 = 0）+ 合規紅線

**Demo 衝刺（6/25 止）**
- [ ] 彩排即時抓「會被金融專業質疑」的點；簡報中金融敘述審一遍（NFR-031 自查）

**如何達成**：6/13 起每天固定 1 小時校題（10 題），用標註指引 v0.2 的格式登記，直接貼群組；紅隊一輪在 6/14 前打完（系統現在就能打，不用等真資料——stub 也要 0 買賣建議）。

---

### R7 — Demo & 全端 🔴 唯一 0 進度角色

**已完成**：無（repo 無前端碼）。但**所有前置已備齊**：開工指南 `docs/R7_frontend_開工指南.md`（含 3 個真實 JSON 契約 + 選型建議）、後端 API 已上線（`POST /ask`、`POST /research`、`GET /alerts`，CORS 已開）、通知中心 demo 頁（#63）可參考。

**未完成（W3，6/16 止）— 範圍已為你砍到最小**
- [ ] 對話 + 引用 UI（打字提問 → 答案 + 來源；接 `/ask`，先 mock 後真連）
- [ ] Alert Inbox（接 `GET /alerts`，契約已定）
- [ ] ReAct trace UI（接 `/research` 回的 `react_steps`，想→查→想 動線）

**未完成（W4，6/19 止）**
- [ ] Citation Tracer（點引用跳原文）＋ 基本 RWD
- [ ] **前端上雲**：Vercel 部署 + 接 Cloud Run 後端 + Landing（R1 文案）
- [ ] ~~Report Viewer / 匯出 PDF~~ → **建議降級為 P2，6/25 後再說**（5 分鐘 demo 用不到）

**Demo 衝刺（6/25 止）**
- [ ] Demo 備援影片（場景預錄）剪輯 + 定稿（6/23–24）
- [ ] 斷網切備援演練（< 30 秒）；Demo Day 技術操作手

**如何達成**：**6/12 今天起工**。選型直接用 Chainlit（開工指南建議，對話 UI 一天可起）或 Next.js + 現成 chat 模板，不要自己刻元件；第一天目標＝本機打 `/ask` 看到答案+引用渲染。每天傍晚丟一張截圖到群組給 R1 簽核（spec 原本就要求截圖驗收）。做不完的順序：對話+引用 > trace > Inbox > 其他全砍。

---

## 2. 日程總表（6/12–6/25）

| 日期 | 里程碑 | 關鍵動作 |
|---|---|---|
| **6/12（五）** | 衝刺啟動 | R4 開跑入庫（先 1 份跑通）；R7 起前端骨架；R6 紅隊開打 |
| **6/13（六）** | **資料解鎖日** | R4 102 份入庫完、`bq-smoke` 綠、群組宣告；ColPali 砍留拍板；R5 跑第一輪真分 |
| 6/14（日） | | R3 檢索接真資料；R5/R6 修題迭代 #1；R7 對話 UI 接 `/ask` |
| 6/15（一） | | R2 Cloud Run 真部署；R3 Rerank 上；R5 題庫到 130；迭代 #2 |
| **6/16（二）** | **🚦 G3 / W3 全收** | R5 公布 Eval 分數（≥80%）；R1 四條件逐項簽核；R3 Watchdog demo 級；R7 三畫面可動 |
| 6/17（三） | | R4 離線備援 + pgvector 驗證；R7 Citation Tracer + Vercel；R5 Eval 報告起草 |
| 6/18（四） | | R2/R4 雲端 4 場景重現測試；R1 部落格 3 篇定稿；R7 接雲端後端 |
| **6/19（五）** | **🚦 G4 / W4 全收** | 4 場景雲端可重現 ✅ 離線備援可切 ✅ Eval 報告 ✅ Landing 上線 |
| 6/22（一） | 簡報週 | R1 簡報大綱 + 5 分鐘腳本 v1；R7 備援影片粗剪 |
| 6/23（二） | 彩排 #1 | 全流程走一次，登問題清單；影片定稿 |
| 6/24（三） | 彩排 #2 | 含斷網切備援演練；**簡報初稿審完** |
| **6/25（四）** | **🎯 團專 Demo + 簡報初稿交付** | 5 分鐘無斷點；簡報初稿定版 |

---

## 3. 風險與降級開關（沿用 spec 既定規則，不另發明）

| 風險 | 觸發 | 降級動作 | 拍板 |
|---|---|---|---|
| R4 入庫 6/14 仍未綠 | 全隊卡死 | R2 直接接手執行入庫，R4 轉做備援資料 | R1，6/14 站會 |
| Eval 6/16 < 80% | G3 No-Go | 砍場景 3 題組 + 「跑分→修最差題→再跑」延至 6/18 補考 | R1+R5 |
| ColPali POC 未啟動（owner R4 卡入庫） | R4 6/14 前無暇啟動 | **R1/R2 backup 接手**：R1 整備素材 + 召集判定、R2 跑 2 天 timebox POC（Colab）；<70% 或 6/14 仍無產出 → 砍場景 3 + ColPali（記 TD-01），檢索 3 路 | R1+R2/R4，6/14 |
| R7 6/16 三畫面未齊 | Demo 沒臉 | 砍到「對話+引用」單畫面 + 其餘用預錄影片頂 | R1，6/16 |
| 新聞第 5 路 / MOPS 真爬蟲不及 | W4 末 | demo-only mock（spec 既定 fallback） | R3/R4 |
| 雲端部署出包 | 6/18 仍不穩 | Demo 切本地 pgvector + 預錄影片（憲法 V 本來就要求備援） | R2 |

> 本表為 2026-06-12 PM 更新；各角色 spec 的 Day 編號自此以本表日期為準。每日站會更新 §0 兩顆紅燈狀態。
