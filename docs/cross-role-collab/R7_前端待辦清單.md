# R7 前端待辦清單

> 整理日期：2026-06-17
> 依賴狀態：✅ 可動工｜⏳ 等 R3 交付｜🔴 阻塞中

---

## 研究助理頁（/research）

### 可動工

| 項目 | 說明 | 狀態 |
|------|------|------|
| 「送同業比較」按鈕 | 點擊後帶著當前 query 跳轉 `/peer?q=...`，讓同業比較頁自動填入公司 | ✅ 已完成 |
| 快速提問 chip（動態） | 已串 `GET /suggestions`，BQ 有資料後自動更新；無資料時走 fallback 靜態 chip | ✅ |
| 完整報告匯出 PDF | `ReportModal` 已實作 | ✅ 已完成 |
| 重新分析按鈕 | 帶相同 query 重新呼叫 `POST /research` | ✅ 已完成 |
| **NFR-031 disclaimer** | 每次研究輸出固定顯示「以下為事實摘要，非投資建議」；前端 hardcoded 顯示，不需等後端。Demo 驗收條件之一（見 Demo 場景草稿） | ⬜ 待做 |
| **邊緣誠實 empty state** | 查詢未入庫的 ticker 或季次時，顯示「查詢的資料未涵蓋於現有資料庫」而非空白或轉圈；Demo 場景 1 邊緣條件，目前行為未確認 | ⬜ 待做 |

### 等 R3 交付

| 項目 | 等待內容 | 狀態 |
|------|----------|------|
| 監控系統警示面板顯示真實資料 | 等 `GET /alerts` 補 `origin` 欄位 | ⏳ |
| 矛盾偵測 → 真實後端 | 等 `POST /contradiction` 端點；交付後移除 client mock | ⏳ |
| 引用追蹤器文件標籤 | 等 PR #6 merge，`doc_type`/`published_at`/`fiscal_period` 正確帶入 | ⏳ |
| 引用追蹤器點開原文 | 等 `GET /chunk/{source_id}` 端點；交付後 DocViewer 顯示 BQ 真實段落與頁碼 | ⏳ |
| 摘要內文顯示真實分析 | 等 BQ 法說稿入庫（R4）+ R3 retriever 運作後，`/research` 回傳的 `final_answer` 才有實質內容 | ⏳ |
| ReAct OBS 顯示真實觀察 | 同上，`react_steps` 內容來自後端 LLM 思考過程 | ⏳ |

---

## 同業比較頁（/peer）

### 可動工

| 項目 | 說明 | 狀態 |
|------|------|------|
| 快速提問 chip 改動態 | 前端 hook 已就緒（`useSuggestions({ mode: "peer" })`），等 R3 交付 `mode=peer` 參數後自動生效 | ⏳ 等 R3 |
| peer-toolbar 公司中文名 | 目前只顯示 ticker；負責人未確認，需釐清是否已完成或由誰接手 | 🔴 確認接手人 |
| **同業比較頁加「示範資料」標示** | mock 數字外觀與真實資料一樣，未標示有 NFR-031 風險；在 `POST /peer-compare` 上線前加一行提示文字 | ⬜ 待做 |
| **同業比較 PDF 匯出暫停或加浮水印** | 整頁為 mock 資料，匯出的 PDF 外觀與真實分析報告相同；待議題 L 開會決定方向（停用按鈕 / 加浮水印 / 維持現狀） | ⬜ 待議題 L 決策 |
| 比較報告匯出 PDF | 同業比較結果匯出為 PDF（與研究助理頁 `ReportModal` 相同元件，帶入比較頁內容） | ✅ |
| **財報期別選擇器** | toolbar 右側新增 `<select>` 讓使用者手選季別（2026Q1 ~ 2024Q4）；query 文字含季別時自動同步；`fiscalPeriod` state 已就緒，等 R3 交付 `POST /peer-compare` 後自動帶入 request body | ✅ 已完成 |
| **Query 自動跳 tab** | `peer.ts` 的 `TAB_KEYWORDS` 擴充至 20 個關鍵字（營收/EPS/獲利 → 財務；逐字稿/說法 → 法說；重大訊息/公告 → 新聞；PE/本益比/PB → 估值）；使用者輸入含維度關鍵字時自動切換 tab | ✅ 已完成 |
| **趨勢折線圖（TrendPanel）** | KPI card 下方新增趨勢面板，顯示兩家公司近 6 季毛利率趨勢對比折線圖；佔位元件已放置，等 R3 交付 `POST /peer-compare` 的 `trend` 欄位後接線 | ⏳ 等 R3 |
| **法說引用可點開** | `CallsBlock` 新增 `onOpen` prop；每筆 quote 帶 `sourceId` 時顯示「查看」按鈕，點擊呼叫 `setOpenDoc` 開啟 DocViewer；`sourceId` 欄位等 R3 交付 `calls.evidence[]` 後自動填入 | ⏳ 等 R3 `GET /chunk/{source_id}` |

### 等 R3 交付

| 項目 | 等待內容 | 狀態 |
|------|----------|------|
| 整頁從 mock 換成真實資料 | 等 `POST /peer-compare` 端點；交付後替換 hardcoded mock | ⏳ |
| ReAct trace 顯示真實步驟 | 等 `/peer-compare` 回傳真實 `react_steps` | ⏳ |
| 引用追蹤器顯示真實引用 | 目前 `citations=[]` hardcoded，等後端回傳 | ⏳ |
| 監控警示顯示真實資料 | 等 `GET /alerts` 補 `origin="peer"` 欄位 | ⏳ |
| 矛盾偵測 → 真實後端 | 與研究助理共用同一 `/contradiction` 端點 | ⏳ |

---

## 通知中心（/notifications）

### 可動工

| 項目 | 說明 | 狀態 |
|------|------|------|
| 「規則管理」改為「訂閱設定」 | 命名邏輯不符實際功能 | ✅ 已完成 |
| 訂閱設定 UI | 讓使用者**主動選擇要追蹤的公司**（multi-select，從 `/companies` 取得清單）；選完呼叫 `POST /subscriptions` 儲存 | ⏳ 等 R3 實作 POST /subscriptions |
| 追蹤通知顯示邏輯 | 訂閱後，呼叫 `GET /tracking-feed?tickers=...` 顯示訂閱公司最新事件（R3 負責後端批次查詢 + 排序） | ⏳ 等 R3 實作 GET /tracking-feed |
| 風險動態 feed | 已整合 `/alerts` + `contraAlertStore`，等 `origin` 欄位補好後即可顯示 | ⏳ R3 |

---

## 新聞頁（/news）

| 項目 | 說明 | 狀態 |
|------|------|------|
| 串接 `events` 資料表 | 改查 `events WHERE event_type='news'`（`GET /events?type=news`，已 live）；前端 `api.news()` 已改呼叫真實端點 | ✅ 已完成 |
| 標題可點擊連結 | 點擊標題跳轉外部 URL（`target="_blank"`），已完成 UI | ✅ 已完成 |
| 顯示邏輯 | 只顯示標題 + 連結，不顯示內文（新聞最多保留一個月） | ✅ 已完成 |

---

## 資料庫頁（/library）

| 項目 | 說明 | 狀態 |
|------|------|------|
| ticker 篩選標籤 | 加入 ticker 維度的分類 tab，顯示中文公司名（從 `/companies` 取得） | ✅ 已完成 |
| doc_type 中文標籤 | 現有 `types` tab 已有部分；確認後端回傳的 `types` 含所有 doc_type | ⏳ 等 R4 入庫 |
| 資料來自真實 BQ | 目前 `/library` 端點待實作；等 R4 入庫後接真實資料 | 🔴 等 R4 |

---

## 對話紀錄頁（/history）

| 項目 | 說明 | 狀態 |
|------|------|------|
| 紀錄寫入 | 使用者每次在研究助理 / 同業比較送出查詢後，寫入 `localStorage`（R7 自行實作，不需後端） | ✅ 已完成 |
| 點擊跳轉 | 點擊紀錄跳轉回對應頁面並帶入 query（已完成） | ✅ 已完成 |

---

## 設定頁（/settings）

| 項目 | 說明 | 狀態 |
|------|------|------|
| Google OAuth 真實串接 | 目前按鈕 UI 存在但無實際認證功能，需串接 Google OAuth | ⏳ 等 R2 決策（見 `docs/cross-role-collab/R2_需求清單_from_R7.md` §1） |
| 工作郵箱 Magic Link | 前端表單已做，需後端 `POST /auth/email` 端點配合 | 🔴 確認後端 |

---

## 共用邏輯（研究助理 + 同業比較共同維護）

| 項目 | 說明 | 狀態 |
|------|------|------|
| **矛盾偵測** | 兩頁面各自有獨立的 `runContradictionCheck()`，觸發同一個 `/contradiction` 端點，結果存在各自頁面的 `contraAlertStore`（以 sessionStorage 隔離，不互相干擾） | ⏳ 等 R3 |
| **監控警示面板** | 兩頁各自過濾 `origin`（research / peer），`contradiction` 兩頁都顯示 | ⏳ 等 R3 |
| **引用追蹤器** | 共用 `CitationList` + `DocViewer` 元件，點擊展開從 `/chunk/{source_id}` 載入原文 | ⏳ 等 R3 |
| **Alert 空白面板說明文字** | `GET /alerts` 回傳 0 筆時目前完全空白，使用者無法判斷是「沒異常」還是「功能壞掉」；加一行「目前無系統警示」說明文字，不需等後端 | ⬜ 待做 |

---

## 優先順序建議

```
R7 可立即動工（不需等後端）：
  1. 🔴 NFR-031 disclaimer — 研究助理頁每次輸出固定顯示「以下為事實摘要，非投資建議」
  2. 🔴 同業比較頁加「示範資料」標示 — mock 數字無標示有 NFR-031 風險
  3. 🟡 Alert 空白面板加說明文字 — 顯示「目前無系統警示」，避免使用者誤以為功能壞掉
  4. 🟡 邊緣誠實 empty state — 查詢未入庫資料時顯示「資料未涵蓋」而非空白
  5. ⬜ peer-toolbar 公司中文名 — 確認接手人並追蹤完成狀態

待議題 L 決策後執行：
  6. 同業比較 PDF 匯出：停用按鈕 / 加浮水印 / 維持現狀（開會決定）

等 R2 決策：
  7. Google OAuth 串接（Auth 框架 + 憑證）
  8. Magic Link 後端（POST /auth/email）
  9. 對話紀錄永久儲存方案（升級 localStorage MVP）

等 R3 PR #6 merge：
  10. 測試引用追蹤器文件標籤

等 R3 交付 /alerts origin 欄位：
  11. 兩頁監控面板顯示真實警示

等 R3 交付 /contradiction：
  12. 移除兩頁的 client mock，改真實呼叫

等 R3 交付 GET /suggestions?mode=peer：
  13. 同業比較頁 chip 顯示比較情境問句（hook 已接線）

等 R3 交付 POST /subscriptions + GET /subscriptions + GET /tracking-feed：
  14. 訂閱設定「儲存」按鈕真實寫入
  15. 追蹤通知 tab 顯示訂閱公司最新事件

等 R3 交付 POST /peer-compare：
  16. 同業比較整頁換成真實資料（同時可移除示範資料標示）

等 R4 交付 GET /library：
  17. 資料庫頁顯示真實文件列表
```
