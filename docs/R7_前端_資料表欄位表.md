# R7 前端 — 後端資料表欄位表（Data Dictionary）

> **給前端**：這份是後端共用庫 `polaris_core` 的「有哪些表、每表有哪些欄、欄是什麼型別、能拿來 filter 哪些值」的單一參照。
> **資料來源**：直接由 live BigQuery schema + 實際值統計產出（2026-06-17 對帳）。
> **權威庫**：`polaris-desk-team.polaris_core`（憲法預設 `VECTOR_BACKEND=bigquery`；pgvector 僅離線/Demo fallback）。
> **唯讀**：前端/一般帳號對 `polaris_core` 只有 READ，**不可寫**（寫入走 SOP §7 PR）。

---

## 0. 一覽（live 列數，2026-06-17）

| 表 | 用途 | 列數 | 涵蓋 ticker | 分區 | 叢集欄 |
|---|---|---:|---:|---|---|
| `chunks` | RAG 語意檢索的文字片段 + 768 維向量 | 6,885 | 20 | `published_at`(DAY) | `ticker, doc_type` |
| `financial_metrics` | 結構化財務指標（EPS／營收…） | 4,368 | 20 | — | `ticker, fiscal_period` |
| `events` | 重大訊息 / 月營收 / 法說會等事件流 | 5,658 | 20 | `published_at`(DAY) | `ticker, event_type` |
| `colpali_pages` | 文件「整頁」視覺向量（ColPali multi-vector） | 5,617 | 20 | — | — |
| `company_dim` | ticker → 公司名 維度表（join 用） | 20 | 20 | `ticker` | ✅ live（2026-06-17 套用）|
| `chunks_pending_reembed` | 內部稽核隔離表（3072 維舊向量） | — | — | — | ⚠️ 前端**請勿使用** |

- 涵蓋的 20 檔 ticker：`1216 2303 2308 2317 2330 2357 2382 2412 2454 2881 2882 2884 2886 2891 2892 3034 3037 3231 3711 6669`。
- `chunks.published_at` 範圍：2025-01-01 ~ 2026-06-16。
- **Join key 一律是 `ticker`**（治理決議 V1_1_JOIN_KEY_TICKER）。公司中文名請 JOIN `company_dim`（見 §5）。

---

## 1. `chunks` — RAG 文字片段（+ 向量）

| 欄 | 型別 | 可空 | 說明 |
|---|---|---|---|
| `chunk_id` | STRING | ❌ NOT NULL | 主鍵，片段唯一 ID |
| `ticker` | STRING | ✅ | 股票代號（join key） |
| `doc_type` | STRING | ✅ | 文件型別，可 filter（見下） |
| `fiscal_period` | STRING | ✅ | 財報期別，如 `2025Q4` |
| `published_at` | DATE | ✅ | 發布日（分區欄） |
| `chunk_text` | STRING | ✅ | 片段全文（顯示 / 引用接地用） |
| `embedding` | ARRAY\<FLOAT64\> | ✅ | 768 維向量（cosine）。**前端通常不用直接抓**，由後端 `/ask`、`/research` 做檢索 |
| `owner` | STRING | ✅ | 存取控制：擁有者帳號；NULL=公開 |
| `confidential` | BOOLEAN | ✅ | 機密旗標；查詢需帶 viewer 過濾 |

- `doc_type` 實際值：`major_news`(5,232)、`transcript`(1,608)、`news`(45)。
- ⚠️ **存取控制**：查 chunks 時後端會加 `(owner IS NULL OR owner=@viewer)` 與 `(NOT COALESCE(confidential,FALSE) OR owner=@viewer)`。前端不要自己拼 SQL 繞過，走後端 API。

---

## 2. `financial_metrics` — 結構化財務指標

| 欄 | 型別 | 說明 |
|---|---|---|
| `ticker` | STRING | 股票代號（join key） |
| `fiscal_period` | STRING | 期別，如 `2026Q2`、`2025Q4`（格式 `YYYYQn`） |
| `metric_id` | STRING | 指標代碼，可 filter（見下） |
| `value` | FLOAT64 | 數值 |
| `unit` | STRING | 單位：`千元`／`新台幣千元`／`新台幣元/股`／`%` |
| `source_id` | STRING | 來源 ID（引用接地） |
| `published_at` | DATE | 發布日 |

- `metric_id` 全集（14 種）：`revenue`、`revenue_yoy`、`revenue_delta`、`revenue_prior_year`、`gross_profit`、`operating_income`、`operating_expense`、`pretax_income`、`net_income`、`eps`、`ytd_revenue`、`ytd_revenue_prior_year`、`ytd_yoy`、`ytd_delta`。
- 一檔股票一期會有多列（每個 `metric_id` 一列）；前端做卡片/表格時用 `(ticker, fiscal_period, metric_id)` 當複合 key 取值。

---

## 3. `events` — 事件流（重大訊息 / 月營收 / 法說會）

| 欄 | 型別 | 說明 |
|---|---|---|
| `event_id` | STRING | 主鍵 |
| `ticker` | STRING | 股票代號（join key） |
| `event_type` | STRING | 事件型別，可 filter（見下） |
| `published_at` | DATE | 發布日（分區欄） |
| `title` | STRING | 標題（列表顯示） |
| `body` | STRING | 內文 |
| `source_url` | STRING | 原始來源連結（引用接地） |
| `raw_json` | JSON | 原始結構化欄位（依 event_type 不同） |

- `event_type` 實際值：`major_news`(5,047)、`monthly_revenue`(340)、`earnings_call`(254)、`news`(17)。
- 適合做「公司動態時間軸 / 通知中心」。

---

## 4. `colpali_pages` — 文件整頁視覺向量（multi-vector）

| 欄 | 型別 | 說明 |
|---|---|---|
| `page_id` | STRING | 主鍵 |
| `ticker` | STRING | 股票代號 |
| `fiscal_period` | STRING | 期別 |
| `doc_type` | STRING | 文件型別 |
| `source_file` | STRING | 原始檔名 |
| `page_num` | INTEGER | 頁碼 |
| `event_date` | DATE | 文件事件日 |
| `published_at` | DATE | 發布日 |
| `embedding` | ARRAY\<FLOAT64\> | ColPali multi-vector |
| `n_patches` | INTEGER | patch 數 |
| `fetched_at` | DATE | 抓取日 |

- 進階「以頁為單位」的視覺檢索用；一般文字問答用 `chunks` 即可。

---

## 5. `company_dim` — ticker → 公司名（✅ live，2026-06-17 套用）

> 已套進 live `polaris_core`（20 列，涵蓋所有事實表 ticker，無 orphan）。前端直接 `JOIN ... USING (ticker)` 取公司中文名即可。來源：`migrations/2026-06-17_create_company_dim.sql`（單一事實來源＝ R6 ontology `02_company`）。

| 欄 | 型別 | 說明 |
|---|---|---|
| `ticker` | STRING NOT NULL | join key |
| `company_name` | STRING | canonical 中文顯示名（如 `台積電`） |
| `english_name` | STRING | 英文名 |
| `market` | STRING | `上市`／`上櫃`… |
| `industry_id` | STRING | 主產業代碼（如 `IND_FOUNDRY`） |
| `industry_name` | STRING | 產業中文名（如 `晶圓代工`） |
| `is_financial` | BOOL | 金融業旗標 |
| `aliases` | STRING | 逗號分隔別名（搜尋用） |
| `company_id` | STRING | 治理參考，**非** join key |

---

## 6. 取數分層（決議：兩者都要）

| 資料 | 走哪裡 | 為什麼 |
|---|---|---|
| **語意問答 / 引用 / research / alert** | 後端 API（`POST /ask`、`POST /research`、`GET /alerts`，見 `src/polaris/api.py`） | 後端處理 embedding、**存取控制（owner/confidential）**、引用接地。前端不碰向量、不碰 `chunks` 原表 |
| **結構化表（財務指標 / 事件流 / 公司清單 / 整頁向量）** | 前端可**直接讀 BQ**：`financial_metrics`、`events`、`company_dim`、`colpali_pages` | 純結構化、無機密欄，用 `ticker`／`fiscal_period`／`doc_type`／`event_type` 當 filter 即可 |

- **API 契約看這裡，別抄**：FastAPI 自動產生，永不過期 —— `GET /openapi.json`、互動文件 `GET /docs`。前端建議用 `openapi-typescript` 從它生 TS 型別，零漂移。
- **顯示用公司中文名**：JOIN `company_dim USING (ticker)`（已 live）。
- **每個數字都要有來源**（憲法引用接地）：帶上 `source_id`／`source_url`／`chunk_id`。
- **🔴 NFR-031**：任何畫面都不得輸出買賣建議。
- ⚠️ **`chunks` 不要前端直讀**：有 `owner`／`confidential` 存取控制，必須由後端帶 viewer 過濾。需要文字片段就走 `/ask`、`/research`。

### 本檔會不會過期？自己驗一下（單一事實來源＝ live BQ）
本表是 2026-06-17 的快照。要確認當下 schema，直接跑（不需寫權限）：
```sql
SELECT table_name, column_name, data_type, is_nullable
FROM `polaris-desk-team.polaris_core.INFORMATION_SCHEMA.COLUMNS`
ORDER BY table_name, ordinal_position;
```
若與本檔有出入，**以 `INFORMATION_SCHEMA` 為準**並回頭更新本檔。

---

*產出方式：`INFORMATION_SCHEMA.COLUMNS` + 值統計，2026-06-17。schema 變更時請重跑上面的查詢核對。*
